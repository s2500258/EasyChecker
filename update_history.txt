EasyChecker Update History

1. Defined the Phase 1 backend-first direction: one event should be ingested, stored in SQLite, and optionally generate one alert.

2. Created the FastAPI backend skeleton under `backend/app` with:
   - `main.py`
   - `config.py`
   - `db.py`
   - `models.py`
   - `schemas.py`
   - route modules for ingest, events, and alerts
   - service modules for ingest logic and rule evaluation

3. Added backend dependency management in `backend/requirements.txt`.

4. Added backend environment configuration support through `backend/.env` and `backend/.env.example`.

5. Implemented SQLite connection handling and table creation for:
   - `events`
   - `alerts`

6. Implemented `POST /api/v1/ingest` to:
   - validate incoming JSON
   - store events
   - evaluate detection rules
   - return the stored event and generated alerts

7. Implemented `GET /api/v1/events` to return events ordered from newest to oldest.

8. Implemented `GET /api/v1/alerts` to return generated alerts ordered from newest to oldest.

9. Added the first backend detection rule:
   - 5 failed login attempts from the same host within 5 minutes -> 1 alert

10. Added `backend/manual_test.py` to send a sample sequence of failed-login events through the ingest endpoint.

11. Added backend automated tests in `backend/tests/test_api.py` to verify:
   - successful event ingest and alert generation
   - validation failure on invalid payloads

12. Installed and used `pytest` for backend verification.

13. Verified the backend end-to-end in-process with FastAPI test tooling because local port binding was restricted in the sandbox environment.

14. Extended the `events` model and API schema with additional normalized security fields:
   - `os_type`
   - `event_code`
   - `category`
   - `username`
   - `ip_address`
   - `raw_data`

15. Added lightweight schema migration logic so existing SQLite databases can receive new event columns through `ALTER TABLE`.

16. Added JSON serialization/deserialization helpers for `raw_data`.

17. Updated backend tests and manual event payloads to include the new normalized fields.

18. Created the Phase 2 agent MVP structure in `agent/` with:
   - `agent.py`
   - `config.py`
   - `collector.py`
   - `sender.py`
   - `schemas.py`
   - `sample_events.py`
   - `requirements.txt`
   - `.env.example`

19. Implemented the first agent execution loop in `agent.py` to:
   - load settings
   - collect events
   - send them to the backend
   - optionally repeat on an interval

20. Implemented agent configuration loading from `agent/.env` with support for:
   - backend URL
   - polling interval
   - hostname
   - OS type
   - event source
   - max events per cycle
   - run-once mode

21. Added a normalized `AgentEvent` model for the agent pipeline.

22. Added payload trimming in the agent so oversized string fields are shortened before sending them to the backend.

23. Implemented the HTTP sender layer in `agent/sender.py` using the Python standard library.

24. Implemented sample event generation for safe end-to-end testing without live Windows logs.

25. Added initial sample agent events for:
   - failed login
   - successful login
   - service state change

26. Expanded the sample agent events with:
   - `process_created`
   - `service_stopped`

27. Added `RUN_ONCE` support so the agent can either:
   - execute one collection cycle and exit
   - continue running in a loop

28. Simplified the agent runtime dependencies so the base agent works mostly with the Python standard library.

29. Added optional Windows-specific dependency support for live collection:
   - `pywin32` on Windows

30. Implemented a live Windows collector in `agent/collector.py` that:
   - targets `Security` and `System` channels
   - reads event IDs `4624`, `4625`, `4688`, and `7036`
   - parses XML event payloads
   - normalizes them into `AgentEvent`

31. Added record state tracking in `agent/.collector_state.json` so already processed Windows events are not re-sent on the next cycle.

32. Added compatibility handling for environments where `win32evtlog.EvtClose` is not exposed.

33. Updated the Windows collector so one unreadable channel (for example `Security`) does not prevent reading other channels such as `System`.

34. Removed misleading sample fallback behavior from live Windows mode:
   - `EVENT_SOURCE=sample` now means sample only
   - `EVENT_SOURCE=windows` now means live Windows events only

35. Performed live Windows-machine testing and confirmed that the current agent is able to collect and send real events into the backend database.

36. Created the Phase 3 frontend MVP in `frontend/` using React and Vite.

37. Added frontend project setup files:
   - `package.json`
   - `vite.config.js`
   - `index.html`
   - `frontend/.env.example`

38. Implemented the frontend app shell and navigation in:
   - `App.jsx`
   - `Layout.jsx`

39. Implemented frontend API access for:
   - `GET /api/v1/events`
   - `GET /api/v1/alerts`

40. Implemented the Dashboard page showing:
   - total events
   - total alerts
   - high alerts
   - unique hosts
   - latest events
   - latest alerts

41. Implemented the Events page with:
   - events table
   - host filter
   - severity filter
   - category filter

42. Implemented the Alerts page with:
   - alerts table
   - host filter
   - severity filter

43. Added reusable frontend UI pieces:
   - metric cards
   - status badges
   - filter bar
   - events table
   - alerts table

44. Added frontend formatting helpers for:
   - timestamps
   - shortened text
   - unique sorted filter values

45. Added MVP frontend styling for the dashboard and table views.

46. Updated `.gitignore` to exclude local runtime artifacts such as:
   - backend and agent `.env` files
   - SQLite database files
   - virtual environments
   - frontend build and dependency folders
   - agent collector state file

47. Rewrote `README.md` so it reflects the current real project state, including:
   - architecture
   - working MVP features
   - environment file handling
   - backend/frontend startup steps
   - correct Windows agent commands
   - sample and live Windows testing flow

48. Added concise English explanatory comments to the agent modules.

49. Added concise English explanatory comments to the backend modules.

50. Added concise English explanatory comments to the frontend modules.

51. Expanded the stub simulation logic so sample/manual data now represents a small fleet of 3 agents on 3 different Windows hosts instead of one repeated host pattern.

52. Reshaped the 30-event simulation into mini incident patterns:
   - `WIN-PC-01` -> brute-force style authentication activity
   - `WIN-LAB-02` -> suspicious process creation activity
   - `WIN-OPS-03` -> service disruption activity

53. Updated `agent/sample_events.py` so fleet sample generation can simulate those host-specific incident patterns.

54. Updated `backend/manual_test.py` so the backend can be fed with 30 simulated events from 3 agents and 3 computers in one run.

55. Added additional backend alert logic to match the simulated fleet scenarios:
   - `Suspicious process burst`
   - `Critical service stopped`

56. Extended backend configuration with rule settings for suspicious process thresholds and time windows.

57. Updated backend ingest flow so rule evaluation receives richer event context:
   - `event_type`
   - `category`
   - `severity`
   - `raw_data`

58. Added frontend table sorting for all key columns except `Message`.

59. Updated the frontend visual style to a more serious cyber security / SOC-like look and later refined it with smaller corner radii and a slightly lighter background gradient.

60. Fixed a frontend dashboard rendering issue caused by sortable table props not being passed on the dashboard page.

61. Improved agent packaging readiness for Windows executable builds by separating:
   - runtime directory
   - `.env` file path
   - collector state file path

62. Reworked collector state persistence for packaged Windows runs:
   - renamed the state file to visible `collector_state.json`
   - added startup logging for runtime, env, and state paths
   - added fallback state storage under `%LOCALAPPDATA%\\EasyCheckerAgent` when the executable directory is not writable

63. Performed packaged Windows `.exe` testing and confirmed that:
   - the executable can run against the backend
   - live Windows events can be collected and sent
   - collector state persistence works through the resolved state file path

64. Documented the Windows `.exe` build flow in the project README.

65. Added a simplified configuration-driven collection layer for the agent with runtime toggles for:
   - login events
   - process events
   - service events

66. Added optional allowlist filtering for:
   - process names
   - service names

67. Updated the agent collector so the same configuration filters apply consistently to:
   - sample-mode events
   - live Windows events

68. Updated `agent/.env.example` and `README.md` to document the new collection controls and allowlist behavior.

58. Added backend automated tests for the new alert patterns and confirmed the backend test suite passes with all current rules.

59. Added frontend table sorting for all columns except `Message`.

60. Updated the frontend visual style toward a more serious cyber security / SOC-inspired look.

61. Reduced UI corner rounding and slightly brightened the dark background gradient.

62. Fixed the dashboard page so preview tables still render correctly after sorting support was added.

69. Added the shared `logo1.png` branding asset to the frontend hero header and reused it as the favicon for:
   - frontend
   - backend
   - backend Swagger UI

70. Adjusted the hero header layout so the logo appears to the left of the `EasyChecker` title and tuned its glow styling for the SOC-like interface.

71. Added lightweight bilingual frontend localization with:
   - English UI copy
   - Finnish UI copy
   - a header language switcher
   - language persistence through `localStorage`

72. Localized key frontend interface areas, including:
   - navigation labels
   - hero text
   - refresh controls
   - dashboard labels
   - events and alerts page headings
   - filter labels
   - table headers
   - loading and empty states

73. Updated the hero description text in both English and Finnish to the newer shorter wording.

74. Added frontend pagination to the `Events` and `Alerts` pages with:
   - default page size of 20
   - page size options 20 / 50 / 100 / all
   - previous / next navigation
   - localized pagination labels

75. Added an `About EasyChecker` page in the frontend and linked it from the bottom of the Dashboard page.

76. Added bilingual service description content to the `About EasyChecker` page, including:
   - author and educational project background
   - a broader explanation of the service functionality

77. Added backend host aggregation through `GET /api/v1/hosts` without introducing a new database table.

78. Added a frontend `Hosts` page with:
   - navigation tab
   - host summary table
   - sorting
   - host / severity / OS filters
   - pagination

79. Added a host activity indicator with two states:
   - `ONLINE`
   - `OFFLINE`

80. Displayed the host activity indicator on the `Hosts` page as a small LED-style dot:
   - green for online
   - red for offline

81. Added host-level IP tracking across the full pipeline:
   - `host_ip` field in the agent event schema
   - `host_ip` storage in the backend `events` table
   - `host_ip` aggregation in the `Hosts` endpoint
   - `Host IP` display in the frontend

82. Improved agent host IP handling by:
   - adding automatic host IP detection based on the backend route and local network data
   - printing the detected host IP at agent startup
   - supporting manual `HOST_IP` override in `agent/.env`

83. Clarified the frontend event display so `Host IP` and `Event IP` are shown as separate columns instead of mixing host-level and event-level addressing.

84. Split the deployment quick-start documentation into platform-specific guides:
   - `QUICK_START_MAC.md`
   - `QUICK_START_WINDOWS.md`
   - simplified `QUICK_START.md` into a short entry page linking to both guides

85. Strengthened brute-force alert detection so it no longer depends on the language of the Windows event message:
   - the backend now prefers normalized event fields such as `event_code=4625` and `category=login_failure`
   - added automated coverage for localized failed-login messages

86. Strengthened critical service alert detection to reduce locale dependence by:
   - normalizing service stop states in the Windows collector
   - deriving stable service keys for important security services
   - updating backend alert rules to use normalized service keys instead of only localized display names
   - adding automated coverage for localized service-stop data

87. Added an expandable alert-rules panel to the frontend `Alerts` page so users can view the currently implemented detection rules directly in the UI.

88. Added persistent alert-to-event linking in the backend through a new `alert_events` table and updated alert API responses to include the exact `event_ids` that formed each alert.

89. Extended the frontend `Alerts` table to show linked event identifiers for each generated alert.

90. Added expandable alert details in the frontend so clicking an alert reveals the full normalized events that contributed to that alert, including timestamps, host, event code, category, severity, user, host IP, event IP, and message.

91. Added configurable brute-force alert settings with backend persistence:
   - introduced rule-setting API endpoints for failed-login thresholds and time windows
   - stored effective values in the backend `rule_settings` table
   - updated alert evaluation to use database-backed values instead of fixed constants

92. Added configurable suspicious-process alert settings with the same rule-setting workflow:
   - backend API support for process-burst threshold and time-window updates
   - persistent storage in `rule_settings`
   - rule evaluation now reads the effective saved values

93. Updated the frontend `Alerts` page so configurable rules use dropdown selectors plus explicit `Apply` buttons instead of saving immediately on every selection change.

94. Added stronger visual feedback for alert-rule updates in the frontend:
   - more prominent `Apply` buttons
   - inline success confirmation after a rule setting is applied

95. Extended the frontend `Events` page filters with:
   - `Host IP`
   - `Time From`
   - `Time To`

96. Refined the frontend filter bar styling to make controls narrower, closer together, and easier to fit into a single horizontal row when space allows.

97. Lightened the visual styling of frontend filter inputs to improve readability within the dark dashboard theme.

98. Added expandable long-message handling to the frontend `Events` table:
   - a `Show all` / `Hide` control in the `Message` column
   - full-width expansion rows for complete event text

99. Refined expanded event-message presentation by:
   - reducing the font size and line height for denser log-style reading
   - switching the expanded message block to a light background with dark text for clearer contrast
